/**
 * Тестовая функция
 * @param a
 * @param b
 */
const someFunc = (a, b) => {
    console.log('something' + a + b);
};

/**
 * Тестовая функция 2
 * @param a
 * @param b
 * @param c
 */
const addNumber = (a, b, c) => {
    return  a + b + c;
};
