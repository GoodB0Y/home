/**
 * Тестовая функция 1
 * @param a
 * @param b
 */
const someFunc = (a, b) => {
    console.log(a + b === 42);
};

/**
 * Тестовая функция 2
 * @param a
 * @param b
 * @param c
 */
const addNumber = (a, b, c) => {
 if (c > 0) {
     console.log('addNumber start')
 }
    return  a + b + c;
};
